function cssSelector(selector) {
  // if(!element || !selector){
  //   return false
  // }
  // split selector & match order by local element
  // const allSelector = CSSwhat.parse('foo[bar]:baz div #qq.ii').reverse();
  //
  // const result = allSelector.every((i) => {
  //
  // })

  console.log('allSelector', allSelector);

  return false;
}
